# Mint/Ubuntu recommended:
#   sudo apt install python3-pyqt6 python3-pyqt6.qtsvg
# (qtsvg provides the landmark/brand vector rendering)
# Optional extras via pip:
#   pip install -r requirements.txt
PyQt6>=6.6
PyQt6-Qt6>=6.6
requests>=2.31
# optional desktop notifications (falls back to notify-send automatically):
plyer>=2.1
